{
  "name": "voice-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Voice/chat agent demo — a deterministic engine makes every decision; the LLM only understands messy input. Runs keyless (engine-only) or with Anthropic/Gemini keys for the NLU fallback layer.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^1.2.12",
    "@google/generative-ai": "^0.21.0",
    "ai": "^4.0.0",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0-beta.7",
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "tailwindcss": "^4.0.0-beta.7",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
